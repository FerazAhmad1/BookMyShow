import { type Request, type Response } from "express"
import { timingSchema } from "../models/shows"
const addTimings = async (req: Request, res: Response) => {
    try {
        const { } = req.body;
    } catch (error) {

    }
}